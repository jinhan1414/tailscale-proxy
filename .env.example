# Tailscale Auth Key — generate a reusable + ephemeral key at:
# https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxxxxxx

# Tailscale hostname — determines the MagicDNS name:
#   <TS_HOSTNAME>.<tailnet-name>.ts.net
TS_HOSTNAME=render-proxy
